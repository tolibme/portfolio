# Simple development Dockerfile - much faster builds
FROM node:22-alpine

WORKDIR /app

# Install dependencies in a single layer
COPY package*.json ./
RUN npm install --no-audit --no-fund

# Copy source code
COPY . .

# Expose port
EXPOSE 3001

# Use development server for faster startup
CMD ["npm", "run", "dev"]